const centerContents = {
  textAlign: 'center',
};

const restrictSize = {
  maxWidth: '100%',
  height: '100%',
  verticalAlign: 'middle',
};

module.exports = {
  centerContents,
  restrictSize,
};
